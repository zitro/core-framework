/** Default framework version pinned by newly scaffolded customer repos. */
export const LATEST_VERSION = "1.4.0";

/** Version of the create-core-discovery-app CLI itself. Stamped into
 * core-discovery.json as cli_version_created and (on upgrade) into
 * cli_version_last_upgrade so customers can tell which CLI touched
 * their repo last. */
export const CLI_VERSION = "1.4.0";
